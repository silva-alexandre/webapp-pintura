{% extends 'base.html.twig' %}

{% block title %}Edit Servico{% endblock %}
{% block body %}
{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
    {{ form_row(form.tipo) }}
    {{ form_row(form.detalhe) }}
    {{ form_row(form.preco) }}
    {{ form_row(form.foto) }}  {# Removendo o input manual e deixando apenas o gerado pelo form #}

    <button class="btn btn-primary">Salvar</button>
{{ form_end(form) }}

{% endblock %}